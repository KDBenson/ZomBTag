using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerUnitPicker : MonoBehaviour
{
    [SerializeField] GameObject blueUnit;
    [SerializeField] GameObject orangeUnit;

    //by default set them both to not active, then pick one to activate, and toggle the activation?

    private void Awake()
    {
        
    }

}
